import 'package:flutter/widgets.dart';

/// Splash Page of the application.
///
/// This page serves as the initial screen or introduction to the app.
/// Typically used to display a logo, animation, or load initial data.
class SplashPage extends StatelessWidget {
  /// Constructor
  const SplashPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
